language: c
compiler:
        - gcc
script: make && sudo make install && bash .test.sh
dist: trusty
notifications:
        irc:
                channels:
                        - "chat.freenode.net#dvdmuckle-bot-test"

